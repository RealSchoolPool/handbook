# Неделя 3 · 22–25.09 — Python, git, первая программа на сервере

Первая неделя ежедневного формата и самая рискованная в программе: **7 студентов из 11
не знают ни одного языка**, а к пятнице у каждой команды должна работать программа
на школьном сервере.

**Цель недели:** не «пройти основы Python», а довести три команды до состояния, когда
у каждой есть репозиторий, коммиты от всех участников и запускающаяся на сервере программа.
Язык даётся ровно в объёме, который для этого нужен.

> **Исключение из правила про 15 минут.** В обычную неделю мини-тема занимает не больше
> четверти часа. На этой неделе объяснение занимает 30–40 минут, потому что мы учим язык.
> С недели 4 возвращаемся к 15 минутам.

---

## Главное упрощение недели

Все три отобранных проекта в первой версии сводятся к одной и той же форме:

```text
  список  →  фильтр  →  печать
```

- учёт ПК: список машин → какие включены → печать;
- Lost&Found: список находок → за какую дату → печать;
- задания/расписание: список заданий → на сегодня → печать.

Значит, **одно объяснение работает на все три команды**, а различаются только данные.
Это и есть тот случай, когда можно учить язык на настоящей задаче, а не на абстрактных примерах.

---

## Что подготовить до вторника

- [ ] **Ревизия сервера** — просрочена. Вторник, слот 14:00–15:00, силами группы B
      (см. ниже). Результат — в `docs/INFRA.md`.
- [ ] Проверить, что на машинах в классе есть Python 3 и хоть какой-то редактор.
      Если нет — это задача первого часа вторника, а не сюрприз среды.
- [ ] Свой walking skeleton, написанный заранее, — чтобы в классе не отлаживать при всех.
- [ ] Аккаунты GitHub у всех: известен один ник. Собрать остальные во вторник — до четверга они обязаны быть.
- [ ] Инициализировать Wiki (одна кнопка), если ещё нет.

---

## Вторник 22.09 — команды и первая программа

**Цель дня: три команды определены, у каждого запустилась своя первая программа.**

Подробный план занятия: **[LESSON-04.md](LESSON-04.md)**.

Коротко: первый час — сравнение двух распределений по командам (их и твоего), дальше — первая
программа на Python. В перерыве третья команда идёт к своему заказчику. Во второй половине
группа B делает ревизию сервера и пишет `docs/INFRA.md`.

**Выход дня:** составы команд в репозитории, у каждого студента отработала своя программа.

---

## Среда 23.09 — фильтр и первые коммиты

Два дня сжаты в один: понедельник выпал, и день «условия» склеен с днём «git». Если не влезает —
git переносится на четверг, а репозитории команд на пятницу. Приоритет у фильтра: без него
у команд нет скелета продукта.

**Цель дня: программа печатает отфильтрованный список, и у каждого есть история изменений.**

| Время | Что |
|---|---|
| 11:00–11:30 | `if` / `else`, список и `for` — минимально, без методов списков |
| 11:30–12:30 | Задания `task03` и `task04` из [lessons/](../lessons/README.md): список → печать, затем список → фильтр → печать. Проверяет `check.py`, а не преподаватель |
| 12:30–13:00 | Зачем нужна история. `git init`, `add`, `commit`, `log` |
| 13:00–14:00 | Перерыв, окно заказчика |
| 14:00–14:40 | Каждый делает репозиторий из своего файла и 3–5 коммитов по ходу правок |
| 14:40–15:00 | **Упражнение «сломай и восстанови»**: испортить файл намеренно, вернуть версию из истории |

**Выход дня:** у каждой команды файл, который печатает отфильтрованный список — это уже скелет
их продукта, сказать это вслух. И каждый хотя бы раз восстановил файл из истории git.

**Про `check.py`:** скопировать каталог `lessons/` на машины до занятия — репозиториев
у студентов ещё нет. Скрипт сам объясняет расхождения и усиливает подсказки с каждой попыткой,
поэтому твоя роль смещается с «проверить» на «подойти к тем, у кого пятая попытка».
Сказать вслух, что попытки не считаются против них: это первое, о чём спросят.

**Не вводить:** ветки, merge, конфликты. Их боль наступит на неделе 4.

**Правило сообщений коммита** — сразу: `add filter by date`, а не `fix`.

---

## Четверг 24.09 — репозитории команд

**Слот сокращён: 11:15–13:00, 105 минут одним блоком.** Подробный план: **[LESSON-06.md](LESSON-06.md)**.

**Цель дня: у каждой команды репозиторий, и у каждого студента в нём коммит со своим именем.**

Первый коммит делается через веб-интерфейс GitHub, а не из терминала: установка и аутентификация
git у тринадцати человек в 105 минут не помещается. Содержимое первого коммита — данные,
собранные в [ДЗ №3](HOMEWORK-03.md).

**Выход дня:** три репозитория, коммит от каждого, у каждой команды общий файл данных
с одинаковыми полями.

Сборка walking skeleton и терминальный git уезжают на пятницу и следующую неделю.

**Группа C курса:** пишут инструкцию по деплою к пятнице и короткую инструкцию «как
закоммитить из консоли» — по ней остальные пойдут позже.

---

## Пятница 25.09 — сервер, демо, ретро

**Цель дня: программа каждой команды запускается на школьном сервере.**

| Время | Что |
|---|---|
| 11:00–11:30 | Что значит «на сервере»: ssh, чужая машина, почему «у меня работало» не аргумент |
| 11:30–13:00 | Деплой по инструкции группы B и по топологии из [CLUSTER.md](CLUSTER.md): свой каталог, `git pull`, ручной запуск, вывод в `logs/`. Каждая команда доводит свою программу до запуска на сервере |
| 13:00–14:00 | Перерыв |
| 14:00–14:40 | **Демо заказчику.** Показывает не тимлид, а тот, кто меньше всех выступал. Показываем запуск, а не рассказ |
| 14:40–15:00 | Ретро: что помогло, что мешало, что меняем на следующей неделе — ровно один пункт. Письменно, в репозиторий |

**Выход недели:**

- [ ] Три репозитория, в каждом коммиты от всех участников
- [ ] Три программы запускаются на школьном сервере
- [ ] Каждый студент хотя бы раз восстановил файл из истории git
- [ ] `docs/INFRA.md` написан
- [ ] Заказчик видел работающий запуск
- [ ] Ретро в репозитории

---

## Роль группы B

Шесть человек уже знают bash, ssh, сети, часть — Python и git. Держать их на «напиши сумму двух
чисел» — быстрый способ их потерять. У них своя линия:

| День | Задача группы B |
|---|---|
| Вт | Ревизия сервера самостоятельно, по чеклисту → `docs/INFRA.md` |
| Ср | Чтение данных из файла + помощь соседям с git |
| Чт | Пишут инструкцию по деплою |
| Пт | Ведут деплой, остальные идут по их инструкции |

Сказать им это прямо во вторник: они не «сильные, которым скучно», а те, на ком держится
инфраструктура. Ответственность удерживает лучше, чем дополнительные задачи.

---

## Если пойдёт не так

| Ситуация | Что делать |
|---|---|
| К среде половина не написала ни одной работающей программы | Остановить продвижение по темам. Git переносится на четверг, скелет — на следующую неделю. Лучше трое умеющих, чем одиннадцать посмотревших |
| На сервере не оказалось Python или доступа | Пятница проводится на localhost, деплой переносится на неделю 4. Это не катастрофа, но `INFRA.md` тогда становится задачей №1 |
| Группа B убежала далеко вперёд | Перевести их в менторы официально: у каждого 2–3 подопечных. Объяснить другому — это и есть их обучение на этой неделе |
| Кто-то принёс код, сгенерированный AI | Правило то же, что и дальше: объясни каждую строку своими словами. Не может — код не едет. Проговорить это во вторник, а не в пятницу |
| Понедельник выпал, не успеваем | Режется среда: git уезжает на четверг, репозитории команд на пятницу, демо становится внутренним. Фильтр не режется — без него у команд нет скелета |
